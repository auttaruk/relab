# ประวัติการทำงาน — FT420 Quant Research Lab (ReLab)

## 2026-09-13 — เริ่มโปรเจกต์ + Phase 0-1 เสร็จ, Phase 2-5 เสร็จบางส่วน (engine พร้อม, ยังไม่มีข้อมูลจริง)

**บริบท:** ผู้ใช้ส่ง mission spec ยาวมากให้สร้าง "FT420 Quant Research Lab" เต็มรูปแบบ (11 phase,
Qlib/Darts/Chronos/TimesFM, agent 8 ตัว, skill 20 ตัว) ใน `aINEWWORLD/ReLab/` ซึ่งตอนเริ่มว่างเปล่า

**การตัดสินใจสำคัญ:** ถามผู้ใช้ก่อนลงมือว่า lab ใหม่นี้ควรสัมพันธ์กับ `quant_system/` เดิม (11GB,
มีฝูงจริง 5 ระบบ deploy อยู่, เอกสาร `บทเรียนที่ได้เรียนรู้.md` ละเอียดมาก) แบบไหน — ผู้ใช้เลือก
**"แยกอิสระเต็มรูปแบบ"** ดังนั้น ReLab **ไม่ import/ใช้ข้อมูลจาก quant_system เลย**

**สิ่งที่สร้างเสร็จ (verified ด้วย pytest 61 tests ผ่านหมด + `python -m cli.lab doctor`):**
- Phase 0: `PROJECT_AUDIT.md`, `LAB_RULES.md`, `AGENTS.md` + `agents/*.md` (8 ตำแหน่ง)
- Phase 1: contracts (`market_data`, `feature_set`, `strategy`, `model_output`, `experiment`,
  `result`, `ft420_export`), engine (`simulator`, `position`, `costs`, `metrics`), CLI
  (`cli/lab.py doctor`, `cli/research.py baseline`, `cli/compare.py`, `cli/promote.py`)
- Phase 2 (บางส่วน): data adapters 3 ตัว — `synthetic_adapter` (test เท่านั้น), `csv_adapter`
  (ปลอดภัย ใช้ได้จริงถ้ามีไฟล์ CSV), `mt5_adapter` (โค้ดพร้อมแต่ **gate ไว้ ห้ามเรียกจริงจนกว่าจะ
  confirm** — เครื่องนี้มี MT5 terminal เดียว เสี่ยงกระทบบัญชี/EA จริงถ้าสลับบัญชี); data quality
  gate (`data/validation/quality.py`)
- Phase 3: baseline 6 ตระกูลครบ (Trend Pullback, Donchian Breakout, Session Breakout, Mean
  Reversion BB/RSI, Volatility Expansion, Structure Breakout) — ทุกตัวรัน end-to-end ได้จริง
- Phase 4: walk-forward (`validation/walk_forward.py`)
- Phase 5: stress test + parameter sensitivity + bootstrap CI + control-group leakage check
  (`validation/{stress_test,sensitivity,leakage}.py`)
- Feature library ครบ 5 กลุ่ม (technical/structure incl. SMC/volatility/session/price_action) —
  64 คอลัมน์จาก OHLCV เดียว
- Promotion gates + scorer + registry writer (`promotion/*.py`) — ทดสอบแล้วให้ผล REJECTED ที่ถูกต้อง
  บน synthetic data (ตามคาด เพราะ random walk ไม่มี edge จริง)
- Skill folder ครบ 20 ตัว มี `SKILL.md` ตามมาตรฐาน (บางตัวยัง "ยังไม่เริ่ม" ตามจริง ไม่ปั้นว่าทำแล้ว)

**บั๊กที่เจอและแก้ระหว่างพัฒนา:**
- `range_expansion` เดิมเทียบ rolling-max-min ของหน้าต่างสั้นกับยาวที่ overlap กัน → ค่าสูงสุด
  ถูกจำกัดที่ 1.0 เสมอทางคณิตศาสตร์ (หน้าต่างสั้นเป็น subset ของหน้าต่างยาว) ทำให้
  `volatility_expansion` baseline ไม่เคยเข้าไม้เลย — แก้เป็นเทียบ range ของแท่งปัจจุบันกับค่าเฉลี่ย
  ย้อนหลัง (shift ไม่รวมแท่งปัจจุบัน)
- Test `test_round_trip_cost...` เขียนผิดเอง (ส่ง entry ที่ปรับ cost แล้วเข้า exit function ซ้ำ) —
  แก้ test ให้ใช้ raw price เดียวกันทั้ง entry/exit

**ยังไม่ได้ทำ (สำคัญ):**
- **ยังไม่มีข้อมูลตลาดจริง** — ทุกอย่างที่รันมาเป็น synthetic (random walk, ไม่มี edge จริง)
  ต้องการอย่างใดอย่างหนึ่ง: (1) ผู้ใช้ export CSV จาก MT5 เอง หรือ (2) authorize ให้เชื่อม MT5
  live แบบระมัดระวัง (ต้อง confirm ทุกครั้งตาม `LAB_RULES.md` #9)
- Phase 6 (ML: Logistic/XGBoost/LightGBM/HMM) — ยังไม่เริ่มเขียนโค้ด
- Phase 7 (Qlib), Phase 8 (Darts/Chronos) — ยังไม่ติดตั้ง dependency, ยังไม่เริ่ม
- Phase 9 (Candidate Factory), Phase 10 (Promotion automation เต็มรูปแบบ), Phase 11 (FT420
  export integration กับ `../web/` จริง) — ยังไม่เริ่ม
- `repo_reports/` ว่างเปล่า — ยังไม่ได้ศึกษา repo อ้างอิงทั้ง 9 ตัวตาม mission (ต้องใช้ WebFetch)
- Leaderboard dashboard artifacts (`leaderboard.csv/json`, `stress_report.json`,
  `ablation_report.json` แบบไฟล์) — ยังไม่ implement เป็นไฟล์ถาวร มีแต่ utility function

**Next:** รอผู้ใช้ตัดสินใจเรื่องแหล่งข้อมูลจริง แล้วจึงรัน baseline 6 ตัวกับข้อมูลจริงผ่าน
`cli.research baseline --source csv`, ตามด้วย `cli.promote` เพื่อดูว่ามีตัวไหนผ่าน gate จริงไหม

---

## 2026-09-13 (ต่อ) — ดึงข้อมูลจริงจาก MT5 + รัน baseline 6 ตัวจริง → REJECTED ทั้งหมด (ตามคาด)

**ผู้ใช้อนุญาต** ให้ดึง MT5 live แบบระมัดระวัง หลังจากถามยืนยันแล้ว

**Safety check ก่อนแตะ MT5 จริง (สำคัญ — บันทึกไว้เป็นแบบอย่าง):**
1. เรียก `mt5.initialize()` **โดยไม่ส่ง credential ใด ๆ** (attach read-only เข้ากับ terminal ที่ล็อกอิน
   อยู่แล้ว ไม่สลับบัญชี) → พบว่าเป็น**บัญชีจริง** (trade_mode=REAL, login 97038507, broker
   "InterStellar MT5", balance $611.13) — คนละบัญชีกับ 97033953 ที่ quant_system เคยบันทึกไว้
2. เช็ค `positions_get()`/`orders_get()` ก่อนทำอะไรต่อ → **0 position เปิด, 0 pending order**
   ปลอดภัยที่จะดึงประวัติราคา (read-only)
3. ใช้เฉพาะ `copy_rates_from_pos`/`copy_rates_range` (อ่านราคาอย่างเดียว) — **ไม่เรียกฟังก์ชัน
   trade ใด ๆ เลย** ตลอด session

**บั๊ก/ข้อค้นพบระหว่างดึงข้อมูลจริง:**
- `copy_rates_range` ของ terminal นี้ไม่เสถียรกับช่วงวันที่กว้าง/เก่ามาก (คืน `Invalid params`
  หรือ clip ไม่สม่ำเสมอ) → แก้โดยเพิ่ม `load_mt5_recent()` ที่ใช้ `copy_rates_from_pos` แทน (เสถียรกว่ามาก
  clip ที่ history root จริงของ symbol เป๊ะ)
- **timezone bug ที่แก้ก่อนใช้จริง**: เวลาที่ MT5 คืนมาเป็น **server time ไม่ใช่ UTC** — พิสูจน์
  เชิงประจักษ์จากแท่งปิดวันศุกร์ก่อนวันหยุดสุดสัปดาห์ (server-labeled 23:58:59 ตรงกับ 21:00 UTC ปิด
  ตลาดมาตรฐานช่วง EDT พอดี ถ้า server=UTC+3) → เพิ่ม `server_utc_offset_hours` (default 3) ใน
  `mt5_adapter.py` แปลงกลับเป็น UTC จริงก่อนเก็บ ตรงกับที่ quant_system เคยบันทึกไว้ค่าเดียวกัน
  (ยืนยันอิสระ ไม่ได้ก็อปมา)
- **บั๊กใน data quality gate เอง (เจอจากข้อมูลจริง ไม่เจอตอนทดสอบด้วย synthetic)**:
  1. `missing_bars_estimate` เดิมนับ gap วันหยุดสุดสัปดาห์ปกติ (~48-96 ชม.) เป็น "ข้อมูลหาย" ทำให้
     score เพี้ยนสุด ๆ (H1 real data ได้ score=0.0) → แก้เป็นหัก allowance 100 ชม. (ครอบคลุมวันหยุด
     ยาว Christmas/New Year ที่วัดได้จริงสูงสุด 96 ชม.) ก่อนนับส่วนเกิน
  2. หลังแก้ข้อ 1 พบว่า H1/M15 เต็มช่วง (2015-2026) ยังไม่ผ่าน เพราะ**ความหนาแน่นข้อมูลจริงต่ำมาก
     ก่อนปี 2023** (2015-2022 มีแค่ ~250-310 แท่ง/ปี ควรมี ~5,700+ ถ้าเป็น H1 ต่อเนื่องจริง) —
     ด่านเดิมจับไม่ได้เพราะแต่ละ gap เดี่ยว ๆ เล็กกว่า threshold เพิ่มเช็คใหม่ `low_density`
     (เทียบจำนวนแท่งจริงกับที่ควรมีถ้าตลาด active ~65% ของเวลา) → จับได้ทันที
- **ข้อมูลที่ใช้งานได้จริง = เฉพาะปี 2023 เป็นต้นไป** (~3.7 ปี) ตรงกับที่ quant_system เคยสรุปไว้เอง
  ("ข้อมูลระหว่างวันของโบรกมีแค่ 3.5 ปี") — ยืนยันอิสระจากคนละบัญชี/คนละ session ว่าเป็นข้อจำกัดจริง
  ของโบรกนี้ ไม่ใช่บั๊กของบัญชีใดบัญชีหนึ่ง

**ข้อมูลที่ cache ไว้แล้ว** (`data/cache/`, ไม่ต้องดึง MT5 ซ้ำ):
- `XAUUSD_v_H1.csv`, `XAUUSD_v_M15.csv`, `XAUUSD_v_D1.csv` — full history ดิบ (มีช่วงปี 2015-2022
  ที่ sparse ใช้ไม่ได้ปนอยู่ **ห้ามใช้ตรง ๆ**)
- `XAUUSD_v_H1_2023plus.csv` (21,658 แท่ง, score=1.0) และ `XAUUSD_v_M15_2023plus.csv`
  (77,911 แท่ง, score=0.93) — **ตัวที่ใช้งานได้จริง ผ่าน quality gate**
- `XAUUSD_v_D1.csv` เต็มช่วงผ่าน quality gate ได้เลย (score=1.0, 2015-04 ถึงปัจจุบัน) เพราะ D1
  ไม่มีปัญหาความหนาแน่น

**ผลรัน baseline จริง 6 ตัว บน XAUUSD H1 2023+ (spread=0.32 จริงจาก symbol_info, lot 0.01,
walk-forward 6/1/1 เดือน, 38 folds):**
```
donchian_breakout       1108 ไม้  pf=0.99  exp_r=-0.0046  DD=33.5R  → ไม่มี edge
trend_pullback           737 ไม้  pf=1.07  exp_r=+0.0407  DD=25.6R  → REJECTED (DD/PF/cost stress ไม่ผ่าน)
session_breakout         371 ไม้  pf=1.00  exp_r=+0.0006  DD=26.0R  → ไม่มี edge (เท่าทุน)
mean_reversion_bb_rsi    603 ไม้  pf=0.95  exp_r=-0.0329  DD=37.2R  → ติดลบ
volatility_expansion    1042 ไม้  pf=0.96  exp_r=-0.0254  DD=53.8R  → ติดลบ
structure_breakout      1544 ไม้  pf=1.04  exp_r=+0.0244  DD=37.7R  → REJECTED (DD/PF/cost stress ไม่ผ่าน)
```
`cli.promote` รัน trend_pullback และ structure_breakout (2 ตัวที่บวก) เต็มด่าน (stress test +
parameter sensitivity + control-group leakage check) → **ทั้งคู่ REJECTED** ผ่าน leakage check และ
parameter stability แต่ตกที่ max_drawdown_r, min_profit_factor, min_expectancy_r, cost_stress —
ตรงกับที่ quant_system เคยสรุปไว้ว่า indicator เดี่ยวธรรมดาบน XAUUSD ไม่มี edge จริง (ยืนยันอิสระ
อีกครั้งด้วยโค้ด/บัญชี/ช่วงเวลาข้อมูลคนละชุด)

**สรุปสถานะ:** Lab ทำงานถูกต้องตามที่ออกแบบไว้ — วิ่งครบ pipeline จริงจาก MT5 → REJECTED ทุก baseline
อย่างมีเหตุผลชัดเจน ไม่ปั้นเลขให้ดูดี ตรงตามเป้าหมายของ LAB_RULES.md ทุกประการ

**Next:** ถ้าจะหา edge ต่อ ต้องไป Phase 6+ (ML/feature ผสม/candidate ที่ซับซ้อนกว่า baseline เดี่ยว) —
baseline เดี่ยวธรรมดาบน XAUUSD H1 ถูกพิสูจน์แล้วว่าไม่มี edge ด้วยกระบวนการที่เข้มงวดของ lab เอง

---

## 2026-09-14 — Phase 7 (Qlib): DEFERRED / Phase 8 (Darts): forecast benchmark + volatility filter

**Phase 7 — Qlib:** เช็ค PyPI ตรง ๆ พบว่า `pyqlib` (Qlib จริงของ Microsoft) มี wheel รองรับแค่
Python 3.8-3.12 เท่านั้น ไม่มี sdist ให้ build เอง เครื่องนี้ใช้ Python 3.13.15 → **ติดตั้งไม่ได้จริง**
ผู้ใช้ตัดสินใจ: **ข้าม Phase 7 ชั่วคราว** (`Phase 7: DEFERRED_ENVIRONMENT_COMPATIBILITY` — ไม่ใช่
FAILED ไม่ใช่ DONE) ไม่ทำ dual-Python/subprocess bridge จนกว่าจะพิสูจน์ว่าจำเป็นจริง — บันทึกเงื่อนไข
กลับมาพิจารณาไว้ใน `skills/qlib_adapter/SKILL.md`

**Phase 8 — Darts:** ติดตั้งสำเร็จ (pure-Python wheel ไม่ชนกับ pandas 3.0.5/numpy 2.5.2 ที่มีอยู่)
สร้าง `models/darts/forecast_targets.py` (future_return/future_range/max_up/max_down/
realized_volatility — forward-looking targets เท่านั้น ไม่ใช่ feature), `models/darts/
forecasters.py`, `models/common/forecast_walkforward.py`, `strategies/candidates/
forecast_filtered.py`, `cli/research.py forecast-walkforward` subcommand ใหม่

**🐛 บั๊กจริงของ Darts เอง (ไม่ใช่โค้ดเรา, เสียเวลาสืบเยอะ บันทึกกันคนต่อไปซ้ำ):**
`darts.models.{LinearRegressionModel,XGBModel,LightGBMModel}.predict()` fail ด้วย
`"the two timestamps provided to slice() have to be of the same type"` บนข้อมูลจริงสเกล
5,000-7,000 แถว (ไม่เกิดกับ synthetic เล็ก) แม้ dtype/index type ตรงกันทุกจุดที่ตรวจสอบได้ —
สันนิษฐานว่า darts 0.47.0 เข้ากันไม่ได้กับ pandas 3.0.5 (ใหม่มาก) **`.fit()` ทำงานปกติ มีแค่
`.predict()` พัง** → ตรวจแล้วว่า Darts' RegressionModel เป็น wrapper บาง ๆ ของ sklearn/xgboost/
lightgbm regressor ตรง ๆ (`lags=None` ทำให้ไม่มี autoregressive) → แก้โดยเรียก regressor ตรง ๆ
แทน (โมเดลตระกูลเดียวกันเป๊ะ ไม่ใช่การหลบเลี่ยง) `darts` ยังติดตั้งอยู่และกำหนด API/vocabulary
ของโมดูลนี้ทั้งหมด

**ผล Forecast Benchmark (single 70/30 split, horizon=24, XAUUSD H1 2023+):**
```
future_return_24:        naive MAE=0.01265, linear=0.01264(เท่า), xgboost/lightgbm แย่กว่า → ไม่มี skill
future_return_6:         เหมือนกัน → ไม่มี skill
realized_volatility_24:  naive MAE=0.00141, linear=0.00094(-33%), xgboost/lightgbm=-26% → มี skill จริง!
```
สรุป: **ทิศทางราคา forecast ไม่ได้เลย** (ตรงกับที่ quant_system เคยสรุป ML ensemble AUC 0.51≈สุ่ม)
**แต่ realized_volatility forecast ได้จริง** (ตรงกับปรากฏการณ์ "volatility clustering" ที่รู้จักกัน)

**ผล Rolling Walk-Forward ของ volatility filter (train12/val3/test3, 10 fold, algo=linear):**
```
donchian_breakout   + vol-above:  median Δexp=-0.0145  positive=50%  → REJECTED
structure_breakout  + vol-above:  median Δexp=-0.0163  positive=80%  pooled CI เกือบไม่ครอบศูนย์ → REJECTED
structure_breakout  + vol-below:  median Δexp=+0.0043  positive=60%  → INCUBATING (ผ่าน 6/8 เกณฑ์)
```
**structure_breakout+vol-below สอบผ่านเชิงกลไก แต่ตรวจละเอียดแล้วอ่อนแรงมาก:** median +0.0043
บางมาก แทบแยกจาก noise ไม่ออก, delta ต่อ fold แกว่ง -0.443 ถึง +0.623, fold ดีสุดมาจากแค่ 6 ไม้
(ไม่ได้ลาก median แต่ก็ไม่ใช่หลักฐานมั่นคง) — **ไม่ควรถือเป็นสัญญาณที่น่าเชื่อจริงจัง** ต้องการ
forward data เพิ่มก่อนพิจารณาต่อ

**สถานะปัจจุบัน:** Phase 6-8 รวมกันทดสอบ ML/forecast filter 6 combinations (donchian x4 algo,
structure x2 algo classification + structure x2 mode forecast) — REJECTED 5, INCUBATING-อ่อนแรง 1
ยังไม่มี ML/forecast strategy ตัวใดผ่านครบแบบไม่มีข้อกังขา — N-BEATS/N-HiTS ยังไม่ลอง (ต้องติดตั้ง
`pytorch_lightning` เพิ่ม รอพิสูจน์ว่าจำเป็นก่อนตามหลักเดียวกับ Qlib)

**Next:** ตัดสินใจกับผู้ใช้ว่าจะ (1) เก็บ forward data ของ structure+vol-below ต่อ (2) ลอง target/
feature อื่น (3) ลองข้อมูลตลาดอื่นถ้ามี (4) ไป N-BEATS/N-HiTS (5) ปิด ML/forecast track แล้วสรุปว่า
XAUUSD H1 ไม่มี edge ที่หาเจอได้ด้วยเครื่องมือที่มี ตรงกับ mission's philosophy "LAB ที่ดีต้องบอกได้
ว่าระบบนี้ไม่ดี"

---

## 2026-09-14 (ปิดงาน) — Prompt 1 Final Acceptance: `PASS_WITH_DEFERRED_OPTIONALS`

ผู้ใช้เลือกข้อ (5) — ปิด ML/forecast track แล้วสรุปเป็น FINAL_REPORT ตามเหตุผลว่า "ยังไม่เจอ edge"
คือผลวิจัยที่ valid และระบบพิสูจน์แล้วว่าคัดของปลอมออกได้จริง ไม่ต้องฝืนหา edge ต่อด้วยของหนัก
(pytorch_lightning/N-BEATS/N-HiTS/ target เพิ่ม)

**ปิดช่องโหว่ก่อนสรุป:** เพิ่ม `tests/test_registry_and_export.py` (registry_writer.py +
ft420_export.py มีโค้ดมาตั้งแต่ Phase 1 แต่ไม่เคยถูก test/เรียกจริงเพราะไม่มี candidate ผ่าน
PROMOTED) — ยืนยันกลไกทำงานถูกต้องด้วย synthetic PROMOTED case

**สร้าง 4 เอกสารปิดงานตามที่ผู้ใช้กำหนด:**
- `FINAL_REPORT.md` — สรุปทุก phase, module, experiment, candidate (ผ่าน/fail/incubating), limitation, next steps
- `PROMPT1_ACCEPTANCE.md` — ตรวจ Definition of Done ครบ 28 ข้อ (19 COMPLETE, 5 PARTIAL, 4 DEFERRED)
  → status `PASS_WITH_DEFERRED_OPTIONALS`
- `RESEARCH_FINDINGS.md` — 5 finding ตามที่ผู้ใช้สั่งเป๊ะ พร้อมตัวเลขรองรับทุกข้อ
- `DEFERRED_ITEMS.md` — 11 หัวข้อที่ตั้งใจไม่ทำ พร้อมเหตุผล+เงื่อนไขกลับมาทำต่อ

**หลักฐาน reproducibility ที่เจอโดยบังเอิญ:** เทียบผล donchian_breakout+linear volatility
walk-forward 2 รอบ (ก่อน/หลังแก้บั๊ก NaN) — fold ที่ไม่กระทบบั๊กให้ตัวเลข**เหมือนกันทุกทศนิยม** ยืนยัน
LAB_RULES.md #1 (dataset เดิม+seed เดิม+config เดิม → ผลเดิม) เชิงประจักษ์ ไม่ใช่แค่ design intent

**การตรวจสอบสุดท้าย:** `python -m cli.lab doctor` ALL GOOD · `pytest -q` → **83 passed** ·
integration test ผ่าน

**สถานะปิดงาน:**
- Core pipeline (Data→Feature→Strategy→Backtest→WalkForward→Stress→Promotion): **ใช้งานได้จริง
  พิสูจน์ด้วยข้อมูลจริงหลายรอบ**
- Candidate ที่ผ่าน: **0**
- Candidate REJECTED: **11** (ทุกตัวมีเหตุผลเฉพาะเจาะจง)
- Candidate INCUBATING_WEAK_EVIDENCE: **1** (structure_breakout+vol-below, ห้าม promote)
- Deferred อย่างมีเหตุผล: Qlib (env), Chronos/TimesFM (ไม่เริ่ม), Candidate Factory/Queue/
  Leaderboard (ยังไม่คุ้มสร้าง), HMM/Ablation (ยังไม่มี candidate ต้องการ), cross-market (รอ
  candidate ผ่านก่อน)

**Prompt 1 ปิดแล้ว ณ จุดนี้** — รอคำสั่งต่อไปสำหรับ Prompt 2 หรืองานใหม่

---

## 2026-09-13 (ต่อ 3) — Phase 6 ML Lab: สร้าง ML confirmation filter + เจอ candidate ที่น่าสนใจ 1 ตัว

**สร้างใหม่:** `models/common/labeling.py` (จำลองผลของทุกสัญญาณ baseline อิสระต่อกัน ใช้
SL/TP/cost เดียวกับ engine จริง), `models/common/train_classifier.py` (feature matrix, 3-way
temporal split, train/evaluate 4 algo: logistic/RF/xgboost/lightgbm), `strategies/candidates/
ml_filtered.py` (wrapper ที่ให้ ML "veto" สัญญาณ baseline เท่านั้น ไม่ตัดสินทิศทาง/ความเสี่ยงเอง —
ยังอยู่ในกรอบ "deterministic logic + trained model" ตาม LAB_RULES.md #6), `cli/research.py ml`
(subcommand ใหม่)

**บั๊กที่แก้ระหว่างทาง:** `features/price_action/patterns.py::candle_range` ใช้ `pd.NA` แทน
`np.nan` ทำให้ `body_ratio` กลาย dtype=object → xgboost fit ไม่ได้ (`ValueError: ... must be int,
float, bool or category`) — แก้เป็น `np.nan`

**บทเรียนวิธีการที่สำคัญที่สุด (เจอจากการทดลองจริง ไม่ใช่ทฤษฎี):**

1. **Train/Val/Test 3 ทางไม่ใช่ทางเลือก เป็นสิ่งจำเป็น** — รอบแรกที่ลอง (2 ทาง, เลือก threshold
   จาก test โดยตรง) donchian+xgboost ดูเหมือนชนะขาด (exp_r ดีขึ้นเมื่อ threshold สูงขึ้น) แต่นั่นคือ
   "optimize จาก test set" ที่ LAB_RULES.md ห้ามไว้ตรง ๆ — แก้เป็น 3 ทาง (train fit โมเดล, val เลือก
   threshold, test ดูครั้งเดียว) แล้วผลเปลี่ยนทันที
2. **ผลที่ "ดูดีมาก" ต้องสอบสวนก่อนเชื่อเสมอ (LAB_RULES.md #6)** — donchian+random_forest ดูเหมือน
   ชนะขาด (test exp_r=+0.387 จาก 30 ไม้) แต่สอบสวนแล้วพบว่า **ไม้ทั้ง 30 เป็น LONG ล้วน** และ
   validation window (ช่วง fit threshold) ทองขึ้น +30.3% รวด → เป็นแค่ trend bias ที่โมเดลเรียนรู้มา
   ไม่ใช่ confirmation skill จริง ยืนยันด้วย bootstrap 90% CI = [-0.075, +0.761] **ครอบศูนย์**
3. เพิ่มเช็คพวกนี้เข้า `cli/research.py::cmd_ml` อัตโนมัติแล้ว: bootstrap CI ต้องไม่ครอบศูนย์ +
   ไม้ต้องไม่ทิศเดียวล้วน + จำนวนไม้ต้องถึง `min_trades_to_trust` ก่อนจะประกาศ "BEATS BASE" —
   กันไม่ให้ผลลวงแบบข้อ 2 หลุดผ่านไปโดยไม่มีใครสังเกต

**ผลสรุป 4 algo (logistic/RF/xgboost/lightgbm) x 2 baseline (donchian_breakout,
structure_breakout) บน XAUUSD H1 2023+:**

- donchian_breakout: **REJECTED ทั้ง 4 algo** (logistic ไม้ไม่พอ, xgboost overfit val, random_forest
  = trend bias ปลอม, lightgbm ไม่ชนะ base)
- structure_breakout + random_forest: REJECTED (ไม้ไม่พอบน validation)
- **structure_breakout + xgboost (threshold=0.55): INCUBATING** 🟡 — candidate ที่น่าสนใจที่สุดของ
  session นี้ ผ่านครบ: ทิศทางสมดุล (23L/25S ไม่ใช่ long-bias), threshold ข้างเคียงให้ผลบวกทั้งแถบ
  (โค้งเรียบ ไม่ใช่ยอดแหลม), bootstrap 90% CI=[+0.211,+0.853] ไม่ครอบศูนย์, ตัด top 5% ยังบวก
  (+0.502), **cost stress แทบไม่กระทบเลย** (+0.559→+0.538 ที่ spread x2+slip x2+comm x1.5,
  `is_fragile=False`), ไม้กระจายสม่ำเสมอ 9 เดือน (ธ.ค.2025–ก.ย.2026)
  ผลบน test ที่ล็อกแล้ว: 48 ไม้, pf=2.27, expectancy_r=+0.557 (base เดิมไม่มี filter = +0.100)

**ทำไมยังไม่ promote (สำคัญ — ห้ามลืม):**
1. มีแค่ 48 ไม้ (`config/promotion.yaml: min_trades: 100`)
2. เทสแค่ train/val/test **1 ชุดเดียว** ยังไม่ใช่ walk-forward หลาย window
3. เทสแค่ **ตลาดเดียว (XAUUSD)** — quant_system เองเคยเจอ CHoCH+sweep+Fib78 ผ่านทุกด่านสถิติบนทอง
   (p=0.0014) **แต่ตกข้ามตลาด 1/9** เตือนตรงเป๊ะว่าผลตลาดเดียวไม่พอเชื่อ แม้จะผ่านด่านอื่นครบก็ตาม

**สถานะ:** `INCUBATING` (ไม่ REJECTED ไม่ PROMOTED) — เก็บโมเดลไว้ที่
`models/xgboost/structure_breakout_filter/`

**Next:** (1) รัน walk-forward หลาย window กับ candidate นี้ (2) หาข้อมูลตลาดอื่น (เช่น EURUSD,
GBPUSD ถ้ามี MT5 history) มาเทสข้ามตลาด (3) เก็บ forward data เพิ่มให้ถึง 100 ไม้ก่อนตัดสินใจ promote

---

## 2026-09-13 (ต่อ 4) — Rolling Walk-Forward พิสูจน์ candidate เดิม REJECTED (ถอนสถานะ INCUBATING)

ผู้ใช้สั่งให้พิสูจน์ structure_breakout+xgboost candidate ให้สุดทางด้วย rolling walk-forward
ก่อนไป Phase 7-8 — สร้าง `models/common/ml_walkforward.py` (refit โมเดลใหม่ทุก fold, เลือก
threshold จาก validation ของ fold นั้นเท่านั้น, ประเมินบน test ของ fold นั้นครั้งเดียว) +
`cli/research.py ml-walkforward` subcommand ใหม่

**รัน train=12mo/val=3mo/test=3mo เลื่อนทีละ 3 เดือน บน XAUUSD H1 2023+ → 10 folds:**
```
fold  test_window          threshold  filtered(L/S)  delta_expectancy_r
0     2024-04→07           0.55       20 (8/12)      -0.109
1     2024-07→10           0.50       14 (9/5)       -0.734  ⚠️ พังหนัก (PF 1.18→0.28)
2     2024-10→2025-01      0.52       16 (8/8)       -0.224
3     2025-01→04           none       fallback        0.000
4     2025-04→07           0.50       41 (19/22)     -0.028
5     2025-07→10           0.60       32 (27/5)      +0.094
6     2025-10→2026-01      0.58       36 (27/9)      +0.218
7     2026-01→04           0.58       40 (28/12)     -0.037
8     2026-04→07           0.50       33 (18/15)     -0.142
9     2026-07→09           0.55       21 (19/2)      +0.672  (ทุนบวกก้อนใหญ่ 1 fold เดียว)
```

**เช็คทั้ง 8 ข้อตามเกณฑ์ที่ผู้ใช้กำหนด:**
```
[PASS] total OOS trades = 365 (>= 100)
[FAIL] positive fold ratio = 60% (ต้อง >= 70%)
[FAIL] median delta expectancy = -0.037 (ต้อง > 0)
[FAIL] median delta PF = -0.076 (ต้อง > 0)
[FAIL] median cost-stressed expectancy = -0.015
[FAIL] fold 1 พังรุนแรง (< -0.5 threshold)
[PASS] threshold stable (range=0.10)
[FAIL] pooled bootstrap 90% CI = [-0.068, +0.215] ครอบศูนย์
```

**VERDICT: REJECTED** — ถอนสถานะ INCUBATING เดิม (ที่มาจาก single 60/20/20 split เพียงครั้งเดียว)

**บทเรียน:** ผล single-split เดิม (48 ไม้, exp_r=+0.557) ดันไปตกช่วง Dec2025-Sep2026 พอดี ซึ่งตรงกับ
fold 6/7/9 ที่เป็นช่วงดีที่สุดของข้อมูลทั้งหมดโดยบังเอิญ พอขยับ test window ไปทั่ว 44 เดือน edge ไม่ทน
โดยเฉพาะ fold 1 (ก.ค.-ต.ค. 2024) ที่ filter ทำให้แย่ลงชัดเจน — ตัวอย่างจริงที่พิสูจน์ว่า single-split
เพียงอย่างเดียวหลอกได้ ต้องมี rolling walk-forward เสมอก่อนเชื่อ candidate ใด ๆ

**Artifact:** `experiments/artifacts/ml_walkforward_structure_breakout_xgboost.json` (fold-by-fold
เต็ม + verdict)

**Next:** structure_breakout+xgboost ปิดตาย (REJECTED ด้วยกระบวนการที่เข้มงวดที่สุดที่ lab มี) —
กลับไป Phase 7-8 (Qlib/Darts) ได้แล้ว โดยมี benchmark ที่แข็งแรงจริง (baseline REJECTED ทั้ง 6,
ML filter REJECTED ทั้ง 2 candidate ที่ทดสอบเต็มรูปแบบ) ให้เทียบ — หรือถ้าจะสู้ต่อในทาง ML ควรลอง

---

## 2026-09-14 (Prompt 1.5) — Research Expansion Pack: เพิ่ม 3 Research Family ใหม่, ปิดงานสถานะ `PASS_WITH_DEFERRED_ITEMS`

หลังปิด Prompt 1 แล้ว ผู้ใช้สั่งงานต่อ "Prompt 1.5 — RESEARCH EXPANSION PACK" ให้เพิ่ม 3 family ใหม่
(Correlation/Cross-Asset, Quant Demand-Supply, Statistical/Arbitrage) โดย**ห้ามแก้ validation
protocol เดิมเด็ดขาด** — ต้อง reuse `run_feature_walk_forward`/`MLFoldResult`/
`evaluate_walk_forward_verdict` จาก Prompt 1 ตรง ๆ ทุก family

### Repo Intake (8 repos)
อ่าน source จริง (ไม่เชื่อ README) ของ 8 repo อ้างอิงที่มิชชั่นระบุ, เขียน `repo_reports/*.md` แยก
"REPORTED, NOT VERIFIED" ออกจาก reproduced ชัดเจน — เจอบั๊ก look-ahead จริง 2 จุดที่ใช้เป็นบทเรียน
ตรงในโค้ดของเราเอง: (1) `mikedconcepcion/momentumfx-research` zone stamp ย้อนหลังเห็นอนาคต 5 แท่ง →
`contracts/zone.py` แยก `formed_at` (descriptive) กับ `confirmed_at` (actionable) เด็ดขาด (2)
`Akshith-Gandham/pairs-trading` เลือกคู่ pair จาก full dataset ไม่ใช่ formation window →
`cli/research_pairs.py` screen คู่ใหม่ทุก fold จาก train window เท่านั้น

### Part A — Correlation Lab: 4 baseline (A-D) บน structure_breakout+XAUUSD/XAGUSD/EURUSD
A=REJECTED (no-op), B=INCUBATING อ่อน (ผ่าน 3/8), **C (Lead-Lag Filter)=INCUBATING ผ่าน 7/8 —
ดีที่สุดในทั้งโปรเจกต์รวม Prompt 1** — threshold stability ผ่านจริง (range=0.15) blocker ที่แท้จริงคือ
pooled bootstrap 90% CI ยังแตะขอบศูนย์ ([-0.0107,+0.1667]) (แก้ไขคำอธิบายผิดที่เคยเขียนไว้ก่อนหน้า —
ดู `CORRELATION_REPORT.md` หัวข้อ 4), D=INCUBATING (delta สูงสุด +0.0324 แต่ threshold ไม่เสถียรจริง
range=1.5)

### Part B — Supply/Demand Lab: zone detector (compression→departure) + 5 baseline (B1-B5)
เจอบั๊กจริงร้ายแรงระหว่างพัฒนา: `cli/research_zone.py` ใช้ touch_events index แบบ global แต่แต่ละ
fold reset เป็น local index → ก่อนแก้ได้แค่ 7 trades จาก 952 touch event จริง (เกือบสูญเสียสัญญาณ
ทั้งหมด) หลังแก้ (`_local_touch_events`/`_local_series` helper) ได้ trade count สมเหตุสมผล
(70/37/36/26/68) — **ผลจริง: ทุก baseline REJECTED** แม้แต่ B1 (fresh touch เปล่า) ก็ยังติดลบ

### Part C — Statistical Arbitrage (Pairs): cointegration + hedge-ratio 3 วิธี (static/rolling/Kalman)
Walk-forward pair screening แบบ formation-only (screen ใหม่ทุก fold, fit hedge ratio จาก
formation เท่านั้น, sweep entry_z บน validation เท่านั้น, score test ครั้งเดียว) — **ทั้ง 3
hedge-method REJECTED ด้วย bootstrap 90% CI ติดลบทั้งช่วง** (static -0.0236, rolling -0.0221,
kalman -0.0179) พบข้อสังเกตเพิ่ม: คู่ US30~GBPUSD/US30~USDCHF (ดัชนีหุ้นข้ามคู่เงิน) แสดง
cointegration p-value ผ่านแต่ half-life สูงผิดปกติ (100-5000+ แท่ง) พร้อม trade ถี่ผิดปกติ — สงสัย
spurious cointegration จาก scale mismatch, บันทึกไว้แต่ไม่แก้ (ตามกฎห้ามช่วย candidate)

### Part E — Triangular Arbitrage Simulator
ดึงข้อมูลจริงเพิ่มจาก MT5 (read-only, ยืนยัน 0 position/order ก่อนทุกครั้ง): EURJPY.v, GBPJPY.v,
EURGBP.v เพื่อประกอบ 3 triangle (USD-EUR-JPY, USD-GBP-JPY, USD-EUR-GBP) reconstruct bid/ask จาก
MT5 spread column จริง — **theoretical edge ติดลบเฉลี่ยทุก triangle เป็นบวกแค่ 0-1.3% ของแท่ง**
(USD-EUR-GBP ไม่เคยเป็นบวกเลยตลอด 18,757 แท่ง) พบ methodology caveat สำคัญ: ที่ execution delay
1-2 แท่ง ตัวเลข "realizable edge" พุ่งสูงถึง 100-228bps แต่เป็น artifact จากความผันผวนราคาปกติ
ไม่ใช่ arbitrage จริง (ยืนยันจาก theoretical_edge ที่ไม่เปลี่ยนเลยไม่ว่า delay เท่าไหร่)

### ปิดงาน
140/140 tests ผ่าน (เพิ่ม 57 test ใหม่จาก 83 เดิม) รวมถึง 2 test integration-level ที่เพิ่มพิเศษ
ปิดช่องว่าง (formation-window screening discipline, z-score normalization causality) — เขียนเอกสาร
ปิดงานครบ 6 ไฟล์ (`RESEARCH_EXPANSION_REPORT.md`, `CORRELATION_REPORT.md`,
`SUPPLY_DEMAND_REPORT.md`, `ARBITRAGE_REPORT.md`, `REPO_INTAKE_REPORT.md`,
`EXPANSION_ACCEPTANCE.md`) — **สถานะสุดท้าย: `PASS_WITH_DEFERRED_ITEMS`** ไม่มี candidate ใด
promoted, deferred item เป็นแค่ test/gate infrastructure เพิ่มเติม ไม่กระทบความถูกต้องของผลวิจัย

**Next:** ถ้าจะกลับมาต่อ — ดู `EXPANSION_ACCEPTANCE.md` หัวข้อ 4 (deferred items) ก่อน โดยเฉพาะ
family-specific promotion gate config ถ้ามี candidate ใหม่ใกล้เกณฑ์ promote ในอนาคต

---

## 2026-09-14 (ต่อ, หลังปิด Prompt 1.5) — Freeze ผลวิจัย + เปิด Lead-Lag Forward Incubation

หลังคุยผลสรุป Prompt 1.5 ผู้ใช้ตัดสินใจ **หยุดไล่หา Research Family ใหม่ชั่วคราว** แล้ว freeze
สถานะวิจัยทั้งหมด (PROMOTED: 0, INCUBATING: 2 — correlation_lead_lag_filter กับ
structure_breakout_vol_below, REJECTED: ที่เหลือทั้งหมด) — เอกสารเต็มที่ `RESEARCH_STATUS_FREEZE.md`

**เปิด 2 track คู่กัน:** (1) Lead-Lag Forward Incubation (เบื้องหลัง งานนี้) (2) Platform Full
Audit/Stabilization (มอบให้ Codex ทำ runtime/production layer ของ FT420 — mobile/desktop sync,
chart realtime, signal state machine, popup/alerts — คนละขอบเขตกับ Research Lab นี้)

**Lead-Lag Forward Incubation:** ผู้ใช้สั่งชัดว่าห้าม retune threshold จาก OOS เดิม (จะกลายเป็น
tuning-on-OOS) — freeze candidate code แล้วให้ forward data จริงเป็นตัวตัดสินแทน สร้าง
`cli/lead_lag_forward_track.py`: hash-verify ไฟล์ frozen 11 ไฟล์ก่อนรันทุกครั้ง (**ปฏิเสธรันถ้ามี
ไฟล์ไหนถูกแก้** — ยืนยันด้วย tamper-test จริงว่าทำงานถูกต้อง), ดึง MT5 สดใหม่, รัน pipeline เดิมซ้ำ,
เก็บเฉพาะ fold ใหม่หลัง freeze date เข้า forward log, พิมพ์ judge report เทียบ 5 เกณฑ์ (expectancy,
threshold stability, long/short balance, regime stability, cost robustness) — รันครั้งแรกวันนี้
ยืนยันว่า mechanism ทำงานถูกต้อง (0/100 forward opportunities สะสม เพราะเพิ่ง freeze วันนี้เอง)

**แก้ไขข้อผิดพลาดสำคัญที่พบระหว่างสร้าง infrastructure นี้:** ตอนตอบคำถามผู้ใช้ "7/8 ยังไม่ดีเหรอ"
ก่อนหน้านี้ ผมอธิบายผิดว่า blocker ของ Lead-Lag คือ threshold instability — พอรัน
`cli/lead_lag_forward_track.py` จริงแล้วอ่าน `verdict.reasons` ตรง ๆ พบว่า**threshold stability ผ่าน
จริง** (range=0.15 ≤ 0.20) ตัวที่ตกจริงคือ **pooled bootstrap 90% CI** ที่ยังแตะขอบศูนย์
([-0.0107,+0.1667]) — แก้ไขแล้วใน `CORRELATION_REPORT.md`, `RESEARCH_EXPANSION_REPORT.md`, และ
`frozen_manifest.json` ทั้งหมด บทเรียน: ต้องอ่าน verdict.reasons ที่โค้ดคำนวณไว้แล้วโดยตรง ไม่ใช่
เดาจาก raw threshold values เอง

**Next:** รัน `python -m cli.lead_lag_forward_track` ซ้ำเป็นระยะ (แนะนำทุกเดือน) เพื่อเก็บ forward
fold ใหม่ — อย่าแก้ไฟล์ frozen ทั้ง 11 ไฟล์จนกว่าจะตัดสินใจ PROMOTE/REJECT candidate นี้แล้ว

---

## 2026-09-15 — DATA-01C XAUUSD Derived Dataset, M15 Validation and Freeze

สร้างชุดข้อมูล immutable XAUUSD_TICK_V1 จาก raw tick CSV 162,067,778 แถว โดยเก็บ
timestamp_raw และแปลง server time Europe/Helsinki เป็น UTC ด้วย IANA/DST rule
สถานะ timezone เป็น **HIGH_CONFIDENCE** เท่านั้น ไม่ยกระดับเป็น VERIFIED

- ตัด bid/ask ที่ใช้ไม่ได้ 5,724,984 แถว และตัด duplicate ที่เป็น raw row ซ้ำติดกัน 9,749
  แถว (จาก duplicate ดิบ 9,805 แถว; อีก 56 แถวอยู่ในกลุ่มที่ invalid อยู่แล้ว)
- ได้ clean tick 156,333,045 แถว, M1 1,115,219, M5 223,296, M15 74,451 และ H1 18,626
  แท่ง เขียนเป็น year/month Parquet ด้วย zstd; primary OHLC คือ mid แต่เก็บ bid/ask
  และ spread statistics ไว้ครบ
- M15 ถูก recompute โดยตรงจาก clean tick แบบ stratified ปีละ 125 แท่ง รวม 500 แท่ง:
  exact match 500, OHLC mismatch 0, tick-count mismatch 0, maximum error 0
- DST/boundary ผ่าน, session completeness ได้ 810 วัน และ 807 วันมี native +20 M15
  bars; capacity-only event detection (ไม่มี P/L หรือ outcome) ได้ 543 เหตุการณ์
  (long 302 / short 241)
- freeze tree hash ของ clean ticks/M1/M5/M15/H1 อยู่ใน
  data/derived/XAUUSD/XAUUSD_TICK_V1/XAUUSD_DATASET_FREEZE.json และมี
  attestation แบบ append-only ที่ artifacts/data_xauusd/DATA01C_FREEZE_ATTESTATION.md

**ข้อจำกัดที่บันทึกตรงไปตรงมา:** collector peak RAM ไม่พร้อม จึงมีค่า sentinel 0
ซึ่งหมายถึง “ไม่ได้บันทึก” ไม่ใช่ใช้ RAM ศูนย์ และ acceptance report รุ่นแรกมี denominator
coverage ผิดเพราะปน Sunday opening session กับ Mon–Fri denominator จึงออก audit V2 แบบ
append-only แก้การแสดงผลโดยไม่แตะ data partition
(artifacts/data_xauusd/DATA01C_COVERAGE_AUDIT_V2.md) ผล paperwork status จึงเป็น
READY_WITH_LIMITATIONS; data validation หลักผ่านและยังไม่มี strategy research ถูกเรียกใช้

**Next:** ถ้าจะสร้าง dataset รุ่นถัดไป ให้ใช้ ID ใหม่และเปิด peak-RAM telemetry พร้อม
self-consistent manifest sealing; หากใช้ V1 ต่อ ให้ reference dataset ID และ tree hashes
จาก freeze attestation ทุกครั้ง

---

## 2026-09-16 — Prompt 1.8D.4 XAUUSD Tick-V1 Breakout Revalidation

รัน clean revalidation บน XAUUSD_TICK_V1 เท่านั้น ใช้ complete-session Asia/London
contract เดิม, next-M15-open entry, 1.5 ATR risk และ native 20-bar path โดยไม่แก้
threshold, ไม่สร้าง feature stack, ไม่ใช้ ML/RL และไม่แตะ frozen partition

- reconstructed 543 เหตุการณ์ตรงกับ DATA-01C dry run (LONG 302, SHORT 241);
  event ID/setup timestamp ไม่ซ้ำ, ไม่มี synthetic bar/forward fill และ 20-bar path ครบ
- EARLY_PATH_V1 32.04%; ECONOMIC_20 54.51%; false-negative 166
  (30.57% ของทุกเหตุการณ์ / 56.08% ของ economic winners)
- ค่า terminal diagnostic แบบ event-level ยังบวกแม้ใช้ observed Bid/Ask spread แต่เป็น
  overlapping-path diagnostic ไม่ใช่ deployable strategy P/L
- ไม่มี feature ใดเป็น ROBUST_INFORMATION หรือ PROMISING_INFORMATION:
  EMA200 distance มี pooled terminal Q5-Q1 +0.564R และ bootstrap 90% บวก แต่ไม่เสถียร
  รายปีและ tail-dependent; break distance, displacement, ADX, EMA อื่น และ tick volume
  ไม่ผ่าน robustness rule ที่ preregistered
- พบ bug ระหว่าง audit: helper quintile เคย drop 84 เหตุการณ์ปี 2023 เพราะ fold ยังไม่มี
  จากนั้นแก้ให้ fold เป็น optional, rerun 10,000-bootstrap ทั้งชุด และยืนยันรายงานปี
  2023–2026 ครบก่อนสรุป ผล branch ไม่เปลี่ยน

**Final:** READY_FOR_1.8E=NO; branch CLOSE_BREAKOUT_ENTRY_BRANCH.
เก็บ false-break/reentry/path/cost findings ไว้เป็น research asset แต่ห้ามนำ breakout
entry filter ไปใช้ต่อ

**Reports:** reports/prompt1_8d4/ และ machine artifact
experiments/artifacts/prompt18d4_tick_v1_revalidation.json

**Next:** เริ่ม family แยกต่างหาก คือ XAUUSD M15 range-reentry mean-reversion
ด้วย preregistration ใหม่; ห้าม rescue breakout entry ด้วย threshold ใหม่

## 2026-09-16 — Prompt 1.9 XAUUSD M15 Range-Reentry Mean-Reversion

ศึกษาครอบครัวใหม่บน `XAUUSD_TICK_V1` แบบ preregistered โดยใช้ Asia/London parent
contract เดิม แต่ไม่กลับด้าน breakout แบบเหมารวม: ตรวจ CLOSE_REENTRY ก่อน แล้วจึงใช้
baseline เดียวคือ next-M15-open ไป midpoint / stop ที่ breakout extreme / 20 bars.

- parent 543; CLOSE_REENTRY ภายใน 40 แท่ง 450 (82.87%); 317 มี native post-entry
  path 40 แท่งครบ จึงเป็น sample เดียวสำหรับ outcome, feature และ economics
- midpoint hit 41.64%, opposite-edge hit 18.30%; median terminal10 +0.005R และ
  terminal20 -0.280R
- gross baseline +0.028R (PF 1.053) แต่ realistic Bid/Ask -0.074R (PF 0.872),
  1.5x -0.120R และ 2x -0.142R; non-overlap เหลือ 315 trades และผลเท่ากัน
- ทั้ง LONG fade และ SHORT fade เป็นลบ; year/fold ไม่เสถียร; tail audit เป็น
  TAIL_DEPENDENT; ไม่มี ROBUST_INFORMATION หรือ PROMISING_INFORMATION
- reentry depth ให้ AUC midpoint สูงสุด 0.659 แต่ Q5-Q1 terminal20 -1.086R และ
  bootstrap 90% คร่อมศูนย์ จึงเป็น geometry diagnostic ไม่ใช่ executable filter

**Final:** `REJECT_RANGE_REENTRY` — ไม่ทำ 1.9B และไม่ rescue ด้วย threshold/ML/grid.

**Reports:** `reports/prompt1_9/`; artifact
`experiments/artifacts/prompt19_range_reentry.json`.

**Next:** เปลี่ยนไปหา strategy family ใหม่ที่ไม่พึ่งการ fade close-reentry ของ Asia range.

## 2026-09-17 — Prompt 3.0 Quant Repo Hunt Round 3 (Forensics + DNA Extraction, ไม่ใช่ backtest เต็มรูป)

Clone + forensic 10 repo ภายนอก (`external_repos/prompt3_0/R30-{1..10}`) ครอบคลุม
8 family: cross-asset lead/lag, cointegration/stat-arb, dynamic hedge ratio,
volatility forecasting, intraday seasonality, tick/microstructure, regime
detection, factor/PCA — ไม่แตะ ICT/SMC/breakout ที่ปิดไปแล้ว

- สกัด 22 hypotheses (`QUANT_DNA.md`) → dedup เหลือ 13 unique
  (`DNA_DEDUPLICATION.md`); พบว่า volatility forecasting, intraday seasonality,
  spread/microstructure, HMM regime เป็น family ใหม่ที่ไม่เคยมีใน ReLab มาก่อนเลย
- **finding สำคัญที่สุด**: ตรวจ `XAUUSD_TICK_V1` จริง (sample 7.28M แถว) พบ
  `last`/`volume` เป็น null 100%, `flags` คงที่ที่ 6 (bid+ask update เท่านั้น) —
  ยืนยันว่าไม่มี trade tape จริงเลย → VPIN/Kyle's lambda/OFI/microprice/IB+CVD ทั้งชุด
  เป็น `NOT_COMPATIBLE` เชิงโครงสร้าง ไม่ใช่แค่ข้อมูลไม่พอ
- freeze 5 READY_NOW candidates ก่อนรัน (`experiments/prompt3_0/READY_NOW_FREEZE.json`
  + sha256) แล้วรัน light discovery จริงบน M1/H1 candles (ไม่ใช่ full validation):
  - **H30_11 HAR-RV**: HAR ชนะทั้ง EWMA และ random-walk (RMSE, QLIKE) บน XAUUSD จริง
    → `INFORMATION_PROMISING`
  - **H30_18 seasonality**: ชั่วโมง UTC ที่ผันผวนสุด/นิ่งสุดเรียนจาก 70% แรกยังคง
    ทำนาย 30% หลังได้ (Spearman ρ=0.83, p=5e-7) → `INFORMATION_PROMISING` (ผลแรงสุด)
  - **H30_13 spread→vol**: correlation เต็มชุดดูดีแต่ sign กลับ out-of-sample →
    `QUANT_WEAK`
  - **H30_14 jump clustering**: ไม่มี effect เลย ทิศทางตรงข้ามด้วยซ้ำ → `QUANT_NEGATIVE`
  - **H30_19 HMM regime**: point estimate สมเหตุสมผลแต่ turbulent-state OOS มีแค่
    2 ตัวอย่าง (จาก 680) → เชื่อไม่ได้ ต้องแก้ design ไม่ใช่ปฏิเสธไอเดีย → `NEEDS_DATA`
  - HMM ใช้ `hmmlearn` ได้แล้ว (เคย `DEFERRED_HMMLEARN_UNAVAILABLE` ใน prompt เก่า)
- Cross-check ประวัติเก่า: H30_04/H30_08 (XAU/XAG cointegration/Kalman) ใกล้เคียง
  candidate D ที่เคย REJECTED ใน Prompt 1.5 — จัดลำดับความสำคัญต่ำกว่า และแนะให้ audit
  Kalman code เดิมหา smoother-vs-filter bug ก่อน re-run
- Multi-asset roadmap (5 อันดับ): XAG lead-lag (เสริม candidate ที่ดีที่สุดในโปรเจกต์
  `correlation_lead_lag_filter`/`INCUBATING_FORWARD`), DXY lead-lag, Kalman hedge
  (audit-first), hedge structural-break filter, correlation-network clustering
- 0 promoted (นอก scope รอบนี้) — ตาม prompt ห้ามใช้คำว่า PROMOTED ใน round นี้

**Reports:** `reports/prompt3_0/` (11 ไฟล์ + `repo_forensics/R30_{1..10}.md`)
**Artifacts:** `experiments/artifacts/prompt30_quant_repo_hunt.json`,
`experiments/artifacts/prompt3_0_light_discovery.json`,
`experiments/prompt3_0/READY_NOW_FREEZE.json` (+ sha256)
**Code:** `research_candidates/prompt3_0/light_discovery.py`

**Next:** `DOWNLOAD_MULTI_ASSET_DATA` — หา XAGUSD tick-level history ก่อน (priority
สูงสุดใน roadmap เพราะเสริม candidate ที่ incubating อยู่แล้วโดยตรง)

---

## 2026-09-17 (ต่อ) — สร้าง XAGUSD_TICK_V1 + time-alignment audit เสร็จ (ตามคำสั่งผู้ใช้)

**บริบท:** ผู้ใช้สั่งให้สร้าง `XAGUSD_TICK_V1` ให้เสร็จ+validate+freeze ก่อน แล้วห้ามเริ่ม
cross-asset research (lead-lag/correlation regime/vol transmission/spread stability ฯลฯ)
จนกว่าจะตรวจ (1) XAU/XAG timestamp overlap (2) timezone/DST consistency (3) missing-period/gap
mismatch ระหว่าง 2 feed ให้เรียบร้อยก่อน — ห้าม optimize threshold ตั้งแต่แรก หา information
structure ก่อน

**สร้าง `XAGUSD_TICK_V1`:**
- raw tick มีอยู่แล้วที่ `Tickdata/XAGUSD/XAGUSD.v_202307211544_202609152119.csv` (87,068,421
  แถว, 3.7GB, format เดียวกับ XAUUSD เป๊ะ — broker suffix `.v` เดียวกัน)
- เขียน `cli/build_xagusd_dataset_v1.py` เป็นฝาแฝดของ `cli/build_xauusd_dataset_v1.py` (DATA-01C
  เดิมทุกประการ — TZ_MODEL=Europe/Helsinki inherited จาก broker/.v feed เดียวกัน, dedup policy,
  M1/M5/M15/H1 aggregation, 500-candle direct validation, DST audit, coverage/gap audit, freeze+
  hash) **ไม่แตะสคริปต์เดิมหรือ dataset XAUUSD เดิมเลย**
- รันจริง (~10 นาที): **READY_FOR_RESEARCH**, clean ticks 79,830,108, M15 direct validation
  **500/500 exact match 0 mismatch**, DST audit **PASS** ทุก transition (2023-2026), M15=74,400
  แท่ง (ใกล้เคียง XAU ที่ 74,451 มาก ตามคาด)
- ไฟล์: `data/derived/XAGUSD/XAGUSD_TICK_V1/` (ticks+candles parquet+
  `XAGUSD_DATASET_FREEZE.json`), `artifacts/data_xagusd/DATA01C_{BUILD_REPORT.json,
  ACCEPTANCE.md}`

**Cross-feed time-alignment audit (`research_candidates/cross_asset_r1/
time_alignment_audit.py`, read-only ต่อทั้งสอง frozen dataset):**
- **พบจุดสำคัญ**: XAG raw export ดึงช้ากว่า XAU ~4 ชม. (XAU M15 จบที่ 2026-09-15T14:15 UTC, XAG
  จบที่ 18:15 UTC) → **overlap window ที่ใช้ได้จริงคือ 2023-07-21T12:30 ถึง 2026-09-15T14:15
  UTC เท่านั้น** ต้อง clip ก่อนเสมอ ห้ามใช้ XAG ช่วงท้ายที่เกินมา
- M15 bar-set agreement ใน overlap window: Jaccard **0.9990** (74,380/74,451 ตรงกัน, XAU-only 71
  + XAG-only 4 แท่ง)
- M1 bar-set agreement: Jaccard **0.9921** (mismatch มากกว่า M15 เพราะทองคำเทรดถี่กว่าเงินจริง
  บางนาทีมี tick ฝั่งเดียว) — **ต้อง inner-join บน `timestamp_utc` เสมอ ห้าม positional align/
  zip สอง dataframe ตรงๆ**
- Gap schedule agreement: **93.62%** (793/819 gap ของ XAU ตรงกับ XAG ที่ bar เดียวกัน — ส่วนใหญ่
  เป็น broker-wide closure ร่วมกัน) แต่มี ~26-28 gap ต่อฝั่งที่เป็น feed-specific (เช่น outage
  เฉพาะสินทรัพย์เดียว) — ไม่ใช่ blocker แต่ต้องรู้ไว้ตอนตีความ gap
- **DST consistency: PASS 100%** — ทุก 6 transition (2023-2026) ให้ bar set เหมือนกันเป๊ะทั้ง
  XAU/XAG ยืนยันว่า TZ_MODEL เดียวกันไม่มี drift เลย
- **Verdict: PASS with mandatory preprocessing contract** — ทุกสคริปต์ cross-asset ในอนาคตต้อง
  (1) clip ทั้งสอง feed เข้า overlap window ก่อนเสมอ (2) inner-join timestamp เสมอ ห้าม assume
  equal-length array (3) รู้ว่า gap ไม่ได้ share กัน 100% (4) DST ไม่ต้องจัดการเพิ่มแล้ว
- ไฟล์: `reports/cross_asset_r1/TIME_ALIGNMENT_AUDIT.md`,
  `experiments/artifacts/cross_asset_r1_time_alignment.json`

**Next:** เปิด Cross-Asset Research Round ตามลำดับที่ผู้ใช้วางไว้ — Hayashi-Yoshida lead-lag map
→ rolling lag stability → lead-lag regime → bar-level confirmation M1/M5 → cost/execution →
walk-forward → bootstrap/tail → decision (พร้อมเก็บ evidence คู่ขนาน: rolling correlation regime,
volatility transmission, relative-strength residual, XAU/XAG spread stability, structural break)
— ยังห้าม optimize entry threshold จนกว่าจะเห็น information structure ก่อน

---

## 2026-09-17 (ต่อ) — Cross-Asset R1 Phase 1: Hayashi-Yoshida lead-lag map เสร็จ

**บริบท:** ผู้ใช้สั่งให้ทำ Phase 1 เท่านั้น (ห้าม optimize threshold, ห้ามสร้างกลยุทธ์, ห้ามใช้ P/L
ตัดสิน) — สร้าง HY lead-lag map ระหว่าง XAGUSD/XAUUSD tick-level พร้อมตอบ 8 คำถามวิจัย preprocessing
บังคับ: clip overlap window, inner-join timestamp เท่านั้น, ห้าม positional align, flag gap-adjacent
period, ห้ามแก้ DST เพิ่ม (ตามที่ audit รอบก่อนสรุปไว้แล้วว่า DST ไม่มีปัญหา)

**โค้ดที่สร้าง** (`research_candidates/cross_asset_r1/`):
- `hy_estimator.py` — clean-room Hayashi-Yoshida contrast estimator (Huth & Abergel 2014),
  vectorised O(n log m) ต่อ lag ด้วย `searchsorted` — **validate แล้วก่อนใช้จริง**: (1) ตรงกับ
  brute-force O(n·m) reference เป๊ะ (2) synthetic lead-lag ที่รู้คำตอบล่วงหน้า (true_theta=4s)
  ประมาณค่าได้ theta*=4.0 พอดี ยืนยัน sign convention ถูก (theta>0 = XAG นำ XAU)
- `hy_daily_scan.py` — สแกนทุก London trading day ใน overlap window (996 วันที่ผ่าน
  MIN_TICKS_PER_DAY=100), lag grid ±60s step 1s, flag gap-adjacent (internal gap>5min หรือ
  preceding gap>3h) — รันจริง 1514s (~25 นาที)
- `hy_daily_scan_finegrid.py` — grid ละเอียด ±5s step 100ms เช็ค sub-second feed-latency
- `hy_session_scan.py` — แยก Asian/London/New York (00-08/08-16/16-24 Europe/London)
- `bar_confirmation.py` — M1/M5 cross-correlation (lag ±10 แท่ง) เป็น confirmation แยกวิธี
- `hy_aggregate_analysis.py` — รวมผลทั้งหมดเป็น `experiments/artifacts/cross_asset_r1/
  PHASE1_HY_SUMMARY.json`

**ผลลัพธ์สำคัญที่สุด — naive per-day peak (argmax) หลอกได้:**
97.3% ของวันมี peak ที่ lag=0 พอดี (ดูเหมือนไม่มี lead-lag) **แต่**เมื่อดูรูปทรง curve ทั้งหมด พบว่า
ไม่สมมาตรรอบ 0 ชัดเจน — contrast(+1s)=0.166 vs contrast(-1s)=0.098 — และ pattern นี้เกิดซ้ำใน
**91.4% ของ 996 วันแยกกัน** (t-test p=7×10⁻¹⁸⁹, Wilcoxon p=8×10⁻¹⁴⁶) ไม่ใช่แค่ค่าเฉลี่ยที่ถูกลาก
ด้วย outlier — ยืนยันซ้ำที่ sub-second grid (100ms) ก็เห็น pattern เดียวกันเรียบๆ ไม่ใช่ artifact
ของ grid หยาบ

**ทิศทาง: XAG นำ XAU** (asymmetric shoulder, ไม่ใช่ peak เดี่ยว) — คงอยู่ทุก session (Asian
95.2%, London 93.0%, NY 90.2%, ทุก p<10⁻¹⁸⁰) และทุกปี (2023-2025 อยู่ที่ 94-98%) แต่**อ่อนลงมาก
ปี 2026** (75.6% ทั้งปี, Q1/2026 เหลือแค่ 51.9% = แทบเป็น noise ไปเลยไตรมาสหนึ่งก่อนจะฟื้นใน
Q2-Q3) — รอด survive การตัด gap-adjacent days ออก (818/996 วัน) แทบไม่เปลี่ยนเลย (91.4%→92.1%)

**ข้อควรระวังสำคัญที่สุด — อาจเป็น feed-latency artifact ไม่ใช่ข้อมูลจริง:**
asymmetry **อ่อนลงในวันที่ volatility/tick สูง** (Spearman ρ=-0.39 กับ RV, ρ=-0.19 กับ tick count,
ทั้งคู่ p<10⁻⁸) — ตรงข้ามกับที่ genuine information flow ควรจะเป็น (วันข่าวแรงควรเห็น lead-lag
ชัดกว่า ไม่ใช่จางกว่า) บวกกับข้อมูลมาจาก broker เดียว (retail OTC/CFD ไม่ใช่ exchange) และ
magnitude เสื่อมลงต่อเนื่อง 3 ปี — ชี้ไปทาง broker pricing-engine latency มากกว่า cross-market
information transmission จริง แต่ไม่มีข้อมูลอิสระมายืนยัน/ปฏิเสธได้ในรอบนี้

**ไม่รอดที่ M1/M5 bar level เลย:** correlation ที่ lag=0 สูงมาก (0.62-0.79 ทุกปี) แต่ lag อื่นๆ
ใกล้ศูนย์หมด — สอดคล้องกับที่ tick-level asymmetry จางหายไปภายใน ~20-30 วินาที ซึ่งสั้นกว่า 1 แท่ง
M1 (60 วิ) พอดี

**สถานะสุดท้าย: `PROMISING_INFORMATION`** (ไม่ใช่ ROBUST เพราะข้อกังขา artifact + อ่อนลงตามเวลา,
ไม่ใช่ WEAK/NO เพราะสถิติแน่นมากและ replicate ข้ามวัน/session ได้จริง) — ยังไม่ใช่สัญญาณเทรดได้
(ตายที่ bar level) next step ที่แนะนำ (ยังไม่ทำ) คือหา tick feed อิสระจาก broker อื่นมา cross-check
ว่า asymmetry นี้เป็นของจริงหรือเป็น artifact เฉพาะ broker นี้

**Reports:** `reports/cross_asset_r1/{HY_LEAD_LAG_MAP,ROLLING_LAG_STABILITY,
YEAR_SESSION_STABILITY,GAP_SENSITIVITY,BAR_CONFIRMATION,FINAL_INFORMATION_VERDICT}.md`
**Artifacts:** `experiments/artifacts/cross_asset_r1/` (raw daily/session/finegrid results +
`PHASE1_HY_SUMMARY.json`)

**Next:** รอผู้ใช้ตัดสินใจ — ยังไม่ optimize threshold หรือสร้างกลยุทธ์ใดๆ ตามที่สั่งไว้ ถ้าจะไปต่อ
Cross-Asset R1 phase อื่น (rolling correlation regime, volatility transmission, relative-strength
residual, spread stability, structural break) ยังไม่ได้เริ่ม

---

## 2026-09-17 (ต่อ) — Cross-Asset R1 Phase 2: volatility transmission XAG→XAU เสร็จ

**บริบท:** ผู้ใช้สั่ง Phase 2 เฉพาะ volatility transmission (ห้ามสร้างกลยุทธ์/ML/P&L/optimize
threshold) — ทดสอบว่า XAG volatility impulse ทำนาย future XAU realized vol ได้ไหม ที่ 5 horizon
(5s/10s/30s/60s/5m) พร้อมเทียบทิศกลับ, year/session stability, gap sensitivity, regime dependence,
normalization, artifact check

**โค้ดที่สร้าง** (`research_candidates/cross_asset_r1_phase2/`):
- `vol_bins.py` — bucket ticks ทั้งสองสินทรัพย์เป็น non-overlapping H-second bins (5 horizon)
  ด้วย `.dt.floor()` pattern เดียวกับที่ validate แล้วใน DATA-01C build script, เก็บ RV
  (sum sq log-return) + log-range + tick count ต่อ bin — **ระวังบั๊ก perf**: เขียนรอบแรกด้วย
  `.apply(lambda...)` ช้ามาก (52s/เดือน) แก้เป็น vectorized `.sum()`/`.agg(["max","min"])` เร็วขึ้น
  ~40 เท่า (77s รวมทั้ง 39 เดือน × 5 horizon)
- `vol_analysis.py` — สร้างคู่ (impulse=RV_XAG ที่ bin t, target=RV_XAU ที่ bin t+1 ถัดไปที่ไม่มี
  gap เท่านั้น) ทั้งสองทิศทาง, pooled Spearman + day-block bootstrap CI (resample วันไม่ใช่แถว
  กันปัญหา overlapping/serial correlation พองนัยสำคัญตาม LAB_RULES §5), quantile table,
  year/session/regime breakdown, gap sensitivity, normalization (own-day-median +
  partial correlation ควบคุม XAU's own past RV), rolling regime, CUSUM structural break
- `vol_fwd_vs_rev_paired.py` — paired day-level test ว่า forward ชนะ reverse จริงไหม (ไม่ใช่แค่
  pooled point estimate ที่ใกล้กันจนหลอกได้)

**ผลลัพธ์หลัก — แข็งแกร่งกว่า Phase 1 ชัดเจน:**
- correlation เพิ่มขึ้นตาม horizon อย่างเป็นระบบ: 0.365(5s)→0.466(10s)→0.627(30s)→0.707(60s)→
  0.778(300s) — bootstrap CI ไม่แตะศูนย์เลยทุก horizon, positive 99.5-100% ของวันทุก horizon
- quantile table ที่ 300s: quintile บนสุดของ XAG impulse ตามด้วย XAU forward vol **สูงกว่า
  quintile ล่างสุดถึง 16.6 เท่า** — dose-response ชัดเจนไม่ใช่แค่ significant นิดหน่อย
- **forward vs reverse**: ที่ 5s เกือบเป็น coin flip จริง (forward ชนะแค่ 43.9% ของวัน, t-test
  p=0.90) แต่ตั้งแต่ 30s ขึ้นไป forward ชนะจริง (62-64% ของวัน, p<0.005 ถึง p<10⁻²¹ ที่ 300s) —
  ทิศทาง "XAG นำ" เชื่อได้ตั้งแต่ ~30s ขึ้นไป ไม่ใช่ที่ resolution ละเอียดสุด
- **เสถียรกว่า Phase 1 มาก**: year stability ไม่มีเสื่อมเลย (2026 แรงสุดหรือเท่าสุดแทบทุก
  horizon), session stability ทุก session ทุก horizon, gap-adjacent exclusion แทบไม่เปลี่ยนอะไร
  เลย (ต่างระดับทศนิยมที่ 4), rolling 20-day window เป็นบวก **100% ทุก horizon** (Phase 1 เคยติด
  ลบสั้นๆ ที่ 2026Q1)
- **regime dependence กลับทิศจาก Phase 1**: ความสัมพันธ์**แรงขึ้น**ในวัน volatility สูง (ทุก
  horizon ถึง 60s) ตรงข้ามกับ Phase 1 ที่อ่อนลง — เป็นลายเซ็นของ information transmission จริง
  ไม่ใช่ feed-latency artifact (artifact ควรจะจางลงเมื่อ tick ถี่ขึ้น ไม่ใช่แรงขึ้น)
- **normalization/partial correlation**: raw correlation ส่วนใหญ่ (55-71%) มาจาก XAU's own
  volatility persistence เอง แต่เหลือ partial correlation จริงที่ควบคุม XAU's own RV แล้วยังบวก
  ชัดเจนทุก horizon (0.11-0.16) — ยืนยันว่า XAG มี incremental information จริง ไม่ใช่แค่ redundant
  proxy ของ XAU's own history

**สถานะสุดท้าย: `ROBUST_INFORMATION`** (สูงกว่า Phase 1's PROMISING_INFORMATION เพราะไม่มีสัญญาณ
artifact แบบ Phase 1 เลย แถมมีลายเซ็นตรงข้าม + เสถียรกว่ามากทุกมิติ) — ผู้ใช้เสนอเอาไปต่อกับ
HAR-RV + seasonality ที่มีอยู่แล้ว (`reports/prompt3_0/`) เป็น "XAU volatility forecast v2" —
รายงานสรุปว่า**สนับสนุนแนวทางนี้** เพราะ partial correlation พิสูจน์ incremental info จริง แต่ยัง
ต้องทำ proper HAR-style multi-lag spec + walk-forward RMSE/QLIKE/HAC-DM ก่อนถือว่าใช้ได้จริง
(ยังไม่ทำในรอบนี้ตามที่สั่งห้าม ML/threshold/strategy)

**Reports:** `reports/cross_asset_r1_phase2/{PHASE2_VOL_TRANSMISSION_MAP,
PHASE2_REVERSE_DIRECTION, PHASE2_YEAR_SESSION_STABILITY, PHASE2_GAP_SENSITIVITY,
PHASE2_REGIME_DEPENDENCE, PHASE2_NORMALIZATION_AND_ARTIFACT_CHECK,
PHASE2_FINAL_VERDICT}.md`
**Artifacts:** `experiments/artifacts/cross_asset_r1_phase2/` (vol_bins_*.parquet ทั้ง 5 horizon +
`PHASE2_VOL_SUMMARY.json` + `PHASE2_FWD_VS_REV_PAIRED.json`)

**Next:** รอผู้ใช้ตัดสินใจ — candidate ที่แข็งแกร่งพอจะเอาไปสร้าง HAR-RV v2 จริงหรือไปต่อ Cross-Asset
R1 phase อื่น (relative-strength residual, spread stability, structural break) ยังไม่ได้เริ่ม
